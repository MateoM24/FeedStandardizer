package com.feedstandardizer.domain.model;

/**
 * Standardized 1X2 market outcome.
 * HOME = "1" (home team win), DRAW = "X", AWAY = "2" (away team win).
 */
public enum Outcome {
    HOME,
    DRAW,
    AWAY
}
